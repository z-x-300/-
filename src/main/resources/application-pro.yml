#生产环境配置
spring:
  datasource:
    username:
    password: 
    url:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.zhangxin.myblog: debug
  file:
    name: myblog-info.log

server:
  port: 8088
